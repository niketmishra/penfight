.DS_Store
node_modules/

# Raw meme downloads the voice pack is cut from. Kept locally so clips can
# be recut (see README), never shipped: assets/voice/ holds what the game
# actually loads.
assets/audio/
