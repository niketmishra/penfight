// Supabase project credentials. The anon key is designed to be public; all
// protection lives in row level security (see supabase/schema.sql).
//
// Setup: create a free project at supabase.com, run supabase/schema.sql in
// the SQL editor, then paste the values from Project Settings > API here.
// Leave both empty and the game still works fully in practice mode.

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
